/**
 * Responses' nullability is validated via Lombok's {@link lombok.NonNull}.
 * Since Lombok annotations aren't present at runtime, we document nullability for OpenAPI explicitly.
 */
package com.samkruglov.base.api.view.response;